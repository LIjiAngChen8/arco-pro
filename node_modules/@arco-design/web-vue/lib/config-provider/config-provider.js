"use strict";
var vue = require("vue");
var context = require("./context.js");
var usePickSlots = require("../_hooks/use-pick-slots.js");
var pluginVue_exportHelper = require("../_virtual/plugin-vue_export-helper");
const _sfc_main = vue.defineComponent({
  name: "ConfigProvider",
  props: {
    prefixCls: {
      type: String,
      default: "arco"
    },
    locale: {
      type: Object
    }
  },
  setup(props, { slots }) {
    const { prefixCls, locale } = vue.toRefs(props);
    const emptySlot = usePickSlots(slots, "empty");
    vue.provide(context.configProviderInjectionKey, vue.reactive({
      prefixCls,
      locale,
      emptySlot
    }));
  }
});
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return vue.renderSlot(_ctx.$slots, "default");
}
var _ConfigProvider = /* @__PURE__ */ pluginVue_exportHelper(_sfc_main, [["render", _sfc_render]]);
module.exports = _ConfigProvider;
