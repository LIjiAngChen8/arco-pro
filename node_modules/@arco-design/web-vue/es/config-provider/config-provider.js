import { defineComponent, toRefs, provide, reactive, renderSlot } from "vue";
import { configProviderInjectionKey } from "./context.js";
import usePickSlots from "../_hooks/use-pick-slots.js";
import _export_sfc from "../_virtual/plugin-vue_export-helper";
const _sfc_main = defineComponent({
  name: "ConfigProvider",
  props: {
    prefixCls: {
      type: String,
      default: "arco"
    },
    locale: {
      type: Object
    }
  },
  setup(props, { slots }) {
    const { prefixCls, locale } = toRefs(props);
    const emptySlot = usePickSlots(slots, "empty");
    provide(configProviderInjectionKey, reactive({
      prefixCls,
      locale,
      emptySlot
    }));
  }
});
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return renderSlot(_ctx.$slots, "default");
}
var _ConfigProvider = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export { _ConfigProvider as default };
